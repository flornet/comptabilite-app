<nav class="navbar is-light is-fixed-top">
  <%= back_link_to_root() %>
  <%= page_title("Statuts pour les factures") %>
</nav>

<div class="action-buttons">
    <div class="field is-grouped">
      <p class="control">
        <%= link_to 'Nouveau statut', new_facture_statut_path, class: "button is-primary" %>
      </p>
    </div>
</div>

<table class="table is-fullwidth is-hoverable">
  <thead>
    <tr>
      <th class="has-text-left">Nom</th>
      <th>Défaut</th>
    </tr>
  </thead>

  <tbody>
    <% @facture_statuts.each do |facture_statut| %>
      <tr class='clickable-row is-clickable' data-href='<%= url_for edit_facture_statut_path(facture_statut)%>'>
        <td><%= facture_statut.nom %></td>
        <td class="has-text-centered">
          <% if facture_statut.defaut %>
          <span class="icon"><i class="fas fa-check"></i></span>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
