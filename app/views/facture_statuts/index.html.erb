<h1 class="title is-4">Statuts pour les factures</h1>
<div class="action-buttons">
    <div class="control is-grouped">
        <%= link_to 'Nouveau statut', new_facture_statut_path, class: "button is-primary" %>
    </div>
</div>

<table class="table">
  <thead>
    <tr>
      <th>Nom</th>
      <th>Défaut</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @facture_statuts.each do |facture_statut| %>
      <tr>
        <td><%= facture_statut.nom %></td>
        <td><%= facture_statut.defaut %></td>
        <td><%= link_to 'Show', facture_statut %></td>
        <td><%= link_to 'Edit', edit_facture_statut_path(facture_statut) %></td>
        <td><%= link_to 'Destroy', facture_statut, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
