<h1 class="title is-4">Taxes</h1>
<div class="action-buttons">
    <div class="control is-grouped">
        <%= link_to 'Nouveau taux de TVA', new_tax_path, class: "button is-primary" %>
    </div>
</div>

<table class="table">
  <thead>
    <tr>
      <th>Nom</th>
      <th>Taux</th>
      <th>Défaut</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @taxes.each do |tax| %>
      <tr>
        <td><%= tax.nom %></td>
        <td><%= tax.taux %></td>
        <td><%= tax.defaut %></td>
        <td><%= link_to 'Show', tax %></td>
        <td><%= link_to 'Edit', edit_tax_path(tax) %></td>
        <td><%= link_to 'Destroy', tax, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
